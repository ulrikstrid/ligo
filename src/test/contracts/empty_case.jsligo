type Bar = {
  kind: 'Bar',
  i: int
};

type Baz = {
  kind: 'Baz'
}

type fooBarBaz =
| Bar
| Baz;

let main = (f : fooBarBaz) : int => {
  switch (f.kind) {
    case "Bar": return f.i
    case "Baz": return -1
  };
}