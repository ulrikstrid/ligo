type Bar = {
  kind: 'Bar',
  value: int
};

type Baz = {
  kind: 'Baz'
}

type fooBarBaz =
| Bar
| Baz;

let main = (f : fooBarBaz) : int =>
  match(f, {
    Bar: (i: int) => i,
    Baz: () => -1
  })